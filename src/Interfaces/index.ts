export interface IOption {
  name: string;
  key: string;
}

export interface IDropDown {
  name: string;
  code: string;
}

export interface INFTCollectionItem {
  id: string;
  imageSrc: string;
  name: string;
  price: number;
}
